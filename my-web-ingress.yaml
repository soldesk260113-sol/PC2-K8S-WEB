apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      # 프론트엔드 (SPA)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-web-svc
            port:
              number: 80

